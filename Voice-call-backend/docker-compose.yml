version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data
      - ./uploads:/usr/src/app/uploads
    restart: unless-stopped
