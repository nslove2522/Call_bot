# Voice Call UI

Simple React + Vite UI for managing campaigns. Environment variables:

- `VITE_API_BASE` - backend base URL (default http://localhost:3001)

Quick start

```bash
cd Voice-call-ui
npm install
npm run dev
```
